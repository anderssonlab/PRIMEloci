% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manipulate_df.r
\name{calculate_thick_df}
\alias{calculate_thick_df}
\title{Calculate Midpoint ("Thick") Positions in a DataFrame}
\usage{
calculate_thick_df(df)
}
\arguments{
\item{df}{A data frame containing genomic intervals with "start" and "end" 
columns.}
}
\value{
A data frame with an additional "thick" column containing the 
  calculated midpoint positions.
}
\description{
This function calculates the midpoint positions, referred to as "thick" 
positions, for each row in a DataFrame containing "start" and "end" columns. 
These columns must be numeric and represent the boundaries of genomic 
intervals.
}
\examples{
df <- data.frame(start = c(100, 200, 300), end = c(150, 250, 350))
thick_df <- calculate_thick_df(df)
}
