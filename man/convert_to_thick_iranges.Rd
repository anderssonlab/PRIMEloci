% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_split_data_basedon_annotation.r
\name{convert_to_thick_iranges}
\alias{convert_to_thick_iranges}
\title{Convert thick.start, thick.end, and thick.width to a thick IRanges Column}
\usage{
convert_to_thick_iranges(grl)
}
\arguments{
\item{grl}{A `GRangesList` object with `thick.start`, `thick.end`, and 
`thick.width` columns in each `GRanges`.}
}
\value{
A `GRangesList` object where each `GRanges` contains a single 
  metadata column `thick` of class `IRanges`.
}
\description{
This function takes a `GRangesList` object, where each `GRanges` contains 
columns `thick.start`, `thick.end`, and `thick.width` as metadata, and 
converts these into a single `thick` column of class `IRanges`.
}
\examples{
# Assuming `grl` is a GRangesList object with appropriate columns
modified_grl <- convert_to_thick_iranges(grl)
}
