% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonoverlapping_ranges.r
\name{wrapup_filter_bed_to_reduce_core}
\alias{wrapup_filter_bed_to_reduce_core}
\title{Process a BED file, filter by score, extract core regions, extend, and save the reduced, non-overlapping ranges}
\usage{
wrapup_filter_bed_to_reduce_core(
  input_bed,
  output_dir = NULL,
  threshold,
  width = 401,
  ext_core = 75
)
}
\arguments{
\item{input_bed}{Character. The path to the input BED file.}

\item{output_dir}{Character. The directory where the output files will be saved.}

\item{threshold}{Numeric. The minimum score threshold to filter ranges.}

\item{ext_core}{Numeric. The extension to apply to core regions (default is 75 base pairs).}
}
\value{
A `GRanges` object containing the reduced, extended ranges.
}
\description{
This function processes a BED file by first converting it to a `GRanges` object, filtering it based on a
specified score threshold, and extracting core regions using the `extract_core` function. The core regions 
are extended left and right to a fixed width of 401 base pairs, and overlapping or adjacent regions are 
reduced using the `reduce()` function. The function saves two outputs: 
1) the selected non-overlapping core regions with the suffix "_reduce_core"
2) the extended and reduced ranges with the suffix "_extended_reduced". 
The results are saved as both RDS and BED files in the specified output directory.
}
